{
    "name": "phaser-rummikub-client",
    "private": true,
    "version": "0.1",
    "description": "Client for Rummikub in Phaser (JS) :)",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "deploy": "wrangler deploy"
    },
    "license": "GPL-3.0-only",
    "devDependencies": {
        "vite": "^7.0.4"
    },
    "dependencies": {
        "phaser": "^3.90.0"
    }
}
